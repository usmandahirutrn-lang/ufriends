## Goals
- Use your uploaded blank background `/assets/premium-blank.jpg` sized 1080×674 for the premium slip.
- Render full-frame in PDF (no cropping) and align overlays to match the "premium sample.jpg" exactly.
- Keep QR fallback when provider doesn’t supply one.

## Template Updates
- Switch premium slip background to `/assets/premium-blank.jpg` and normalize backslashes.
- Set exact page size using CSS: `@page { size: 1080px 674px; margin: 0 }` and `html,body { margin:0; padding:0 }`.
- Use a single HTML document (no nesting) so CSS page size is honored by the PDF engine.

## Overlay Elements (Positions + Styling)
- Static labels (overlayed as text):
  - Top-left header: `FEDERAL REPUBLIC OF NIGERIA` (green, bold) and `DIGITAL NIN SLIP` under it.
  - Left panel labels: `SURNAME/NOM`, `GIVEN NAMES/PRENOMS`, `DATE OF BIRTH`, `SEX/SEXE`, `ISSUE DATE`.
  - `NGA` aligned near the QR bottom-left as in sample.
  - Bottom-left: `National Identification Number (NIN)` label.
  - Right panel: Disclaimer text block (title "DISCLAIMER", subtitle "Trust, but verify", body lines and "CAUTION!"), matching font weights and sizes.
- Dynamic placeholders:
  - Photo: `{{image}}` with `object-fit: cover`.
  - QR: `{{qr_code}}` with fallback generation if missing.
  - Surname: `{{surname}}` (uppercase, bold).
  - Given names: `{{given_names}},` (uppercase, bold, trailing comma).
  - DOB: `{{dob}}` (uses mapped aliases from `birthdate/dateOfBirth`).
  - Gender: `{{gender}}` (uppercase).
  - Issue date: `{{issue_date}}`.
  - NIN (spaced): `{{nin_spaced}}{{nin}}` (shows grouped digits when length=11).
- Fonts: use Arial/Helvetica to match sample, sizes tuned per area (header, labels, values).

## Coordinate Strategy
- Convert absolute 1080×674 pixel positions from the sample into template CSS using pixel coordinates for exactness.
- Keep values in CSS variables to allow simple fine-tuning later without code churn.
- Use `object-fit: contain` for background; `cover` for photo; `contain` for QR.

## Builder Integration
- Builder already supports intrinsic image sizing; set canvas to 1080×674 when previewing this background.
- If desired, save a customized template from the Builder; otherwise the resolver auto-provides this aligned premium template.

## Data Mapping & QR Fallback
- Ensure aliases for `dob`, `nin_spaced`, `qr_code` are active.
- QR fallback encodes `{ nin, surname, given_names, dob, issue_date }` into a small PNG data URL.

## Verification
- Generate a premium NIN PDF using the new template; confirm full-frame rendering and alignment.
- Iterate coordinates by a few pixels where needed to match your sample exactly.

## Deliverables
- Updated premium template HTML with precise overlays.
- No changes required to your UI flow; slip type “Premium” continues to resolve the aligned template.

## Confirmation
- Confirm this plan and I will implement the template switch to `/assets/premium-blank.jpg`, set page size to 1080×674, overlay all labels/placeholders, and tune positions to match your sample precisely.